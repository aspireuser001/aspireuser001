{
  "name": "businessos-monorepo",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm --workspace apps/api run dev",
    "build": "npm --workspace apps/api run build",
    "start": "npm --workspace apps/api run start"
  }
}

